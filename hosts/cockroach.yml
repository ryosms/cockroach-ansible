---
front:
  hosts:
    front:
      ansible_host: front.example.com
      ansible_user: root
      ansible_ssh_private_key_file: ~/.ssh/your-key.pem
      # if you use healthchecks.io ( https://healthchecks.io ), add your ping-token below
      yum_update_healthchecksio_token:
cockroach:
  hosts:
    db1:
      ansible_host: db1.example.com
      # if you use healthchecks.io ( https://healthchecks.io ), add your ping-token below
      yum_update_healthchecksio_token:
    db2:
      ansible_host: db2.example.com
      # if you use healthchecks.io ( https://healthchecks.io ), add your ping-token below
      yum_update_healthchecksio_token:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/your-key.pem
